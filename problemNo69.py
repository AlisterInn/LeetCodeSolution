class Solution(object):
    def mySqrt(self, x):
        """
        :type x: int
        :rtype: int
        """
        return int(x**0.5)
    
    """They say that this is not valid but I just started to work in pyhton so I think this is the only way, if someone else have any other solution.
    Please let me know, of how to do this in more of a abstract way. I'm posting this only because the leetcode passed it."""
